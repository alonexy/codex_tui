# Codex 项目协作约定

本项目以可独立验收的端到端交付能力划分任务，主 Agent 负责拆解质量。

## 按角色读取

- 主 Agent：开始实现、修复、重构或协作演练时，读 `docs/agents/decomposition.md` 决定分工；自留工作扩大或准备接管交付时重新划界。该流程允许有价值的子 Agent 委派及交付 Agent 的下级委派。
- 下级 Agent：按派工消息中的角色和任务包执行，读取 `docs/agents/handoff.md`；无需执行主 Agent 的全局拆解流程。
- 派工、接收结果、重试或恢复工作时，派工父 Agent 读取 `docs/agents/handoff.md`，执行上下文筛选、任务去重和证据复用规则。
- 解释或咨询直接回答；审查请求保持审查范围；用户明确要求单 Agent、静态审查或仅出计划时立即遵守。

## 共同边界

- 用户目标与已确认约束优先。按交付能力组织工作，不按前端、后端、测试建立固定部门。
- 主 Agent 维护任务计划和共享契约；交付 Agent 拥有完整能力的实现与验证责任，可以亲自修改代码。
- 同一共享资源在同一时间只有一个写入负责人；先检查现有改动，保留用户和其他 Agent 的工作。
- 协作记录保持轻量：任务 ID、负责人、当前状态、已完成/剩余工作与证据即可；按需记录变更原因，不要求计划版本号、文件哈希或额外版本控制系统。
- 角色与嵌套深度以实际工具能力为准。委派不可用时在当前 Agent 完成，并明确报告降级；不伪造父子关系或验证结果。
- 各角色继承当前会话的模型与权限。本项目指令不授权发布、部署、发送消息或扩大权限。
