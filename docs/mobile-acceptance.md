# 手机操作验收记录

日期：2026-09-15。负责人：主 Agent；项目/命令与设备/安全记录分别交付，主 Agent 集成并进行组合验证。
范围：本轮六项手机体验优化。HTTPS 部署仍由用户后续安排，手机真机验收不以浏览器尺寸模拟代替。

## 限定文件附件增补（2026-09-16）

- 只扩展文本/Markdown/日志/代码/JSON/CSV 及 PDF、Word、Excel、PPT，保留 PNG/JPEG/WebP；不开放 ZIP、音视频、HEIC 或宏启用 Office 后缀。文本要求 UTF-8，Office 检查容器及主文档类型，拒绝识别出的宏/加密内容；部分复杂旧版 Office 容器需另存为新版格式。
- 普通文件 20 MiB、图片 8 MiB，每条消息共 10 个附件、合计 50 MiB。前后端检查单件大小，服务端按登录会话隔离批次并发配额，并在 turn 提交前再次核验实际附件。上传使用独立二进制接口，原件私有随机存储，消息仅传原生 `mention`/`localImage` 引用。
- 文件卡片展示名称、大小、上传进度和失败状态；失败保留草稿、复用成功上传项。仅文件也可发送，历史文件卡片可登录后下载；名称作为文本展示，下载强制附件及 `nosniff`，不开放任意本机路径。兼容旧图片上传接口。
- `npm test`：126/126 通过；`scripts/file-attachments-smoke.mjs` 的 320/390px 验证及 `media-smoke.mjs`、`live-message-smoke.mjs`、`mobile-smoke.mjs` 回归通过。覆盖类型与配额、登录及来源限制、原件字节、恶意名称/路径、失败重试、会话隔离和未知回执不重放。证据保存在本地 `output/playwright/file-attachments/`。
- 已仅重启 Web。真实接口验证 TXT/PDF 上传与下载字节一致，未登录下载被拒绝，ZIP 被拒绝；五个页面资源与磁盘一致且 `no-store`。验证样例已删除、验证登录已退出，桌面桥接 PID/标识未变且保持就绪。
- 未发送真实模型任务，未做 iPhone 真机验证。文档提取和 OCR 需任务使用可用工具，本功能不内置解析器或自动识别；不扩大任务权限。原件暂无自动清理，未发送草稿仍只保存在当前页面内存。

## 会话归档与图标入口增补（2026-09-16）

- 侧栏重命名改为铅笔图标，增加归档入口及目标确认；“会话范围”可切换未归档和已归档，恢复后再打开会话。动作保留无障碍名称及至少 44px 触摸区域。
- 原生 `thread/archive`、`thread/unarchive` 与归档过滤列表通过同一桌面桥接执行；操作使用提交回执，失败或结果未知不自动重发。运行中、等待处理、连接未就绪及旧桥接均显示不可用原因。
- 归档当前会话后清空选择，内存中的文字和图片草稿保留；非当前会话归档不改动当前草稿。归档项取消本地收藏，恢复后不会擅自重新收藏。归档列表不能直接恢复执行。
- `npm test`：121/121 通过；归档专项与原有命名浏览器回归通过，覆盖 320/390px 图标触摸区域、确认与取消、失败、回执恢复不重发、桌面通知、旧桥接提示及草稿保留。断线与事件重置后的旧选择重新核对；回执丢失可人工核对后解除本地锁定。
- 复验脚本：`scripts/session-archive-smoke.mjs`、`scripts/session-names-smoke.mjs`，以模拟 API 验证，不写入真实会话。日志和截图位于本地 `output/playwright/session-archive/`。真实 Web 的页面资源与磁盘一致且 `no-store`，验证登录已退出，桥接进程及标识未变、仍就绪。
- 本机只读核对：当前驻留桥接仍未声明 `taskArchive`。代码更新后需要在任务结束时正常退出桌面，再执行 `npm run desktop` 加载新版桥接；仅重启 Web 无效。本轮不操作真实会话归档，不重启桌面；真实 iPhone 与桌面双端归档仍待实测。

## 按项目创建会话增补（2026-09-16）

- 默认只选项目即可创建，起始目录放入默认折叠的高级选项。`cwd` 可自动选择或调整，但 `runtimeWorkspaceRoots` 始终包含所选项目的全部目录，包含位于不同父目录的组成部分；不以共同父目录替代精确范围。
- 保留项目归属、可选名称、起始目录偏好与创建/命名回执恢复；手动目录入口仍为单目录会话。目录失效或项目尚未同步时阻止创建，不悄悄落到其他路径。已有会话不自动改变范围。
- 验证：`npm test` 113/113；`scripts/session-names-smoke.mjs` 和 `scripts/mobile-smoke.mjs` 通过。覆盖默认只选项目创建、跨父目录参数、可选起始目录不缩小范围、失效目录阻止提交、320/390px表单、命名与失败恢复。
- 日志及截图：本地 `output/playwright/project-workspace/`。运行中的 `app.js` 和 `index.html` 已核对与磁盘一致，独立验证登录已退出，刷新页面即可，无需重启。
- 未创建真实桌面测试会话，真实 iPhone 和实际跨目录任务执行尚未实测。

## 执行过程折叠增补（2026-09-15）

- 每轮只有一个默认收起的执行过程入口，明确的中间进度和工具记录在内，用户消息与最终回复在外；未标注消息阶段的旧回复保持正文可见。
- 摘要在执行时显示最新动作，完成时使用原生整轮时间及已确认修改记录；不累加工具耗时，不从助手口述猜测文件数量。缺少元数据或历史只加载一部分时，统计必须省略或明确限定范围。
- 手动展开后，实时更新、完成及历史刷新保留开关状态；审批、提问、轮次失败与工具异常不能因折叠而不可见。展开后的长命令和差异仅在详情内滚动。
- 已完成测试 112/112；320/390px隔离 Chrome 验证单折叠、正文/问题外显、长输出、真实字段统计、完成与旧读取竞态、展开及焦点保留、分页、切换、断线恢复、草稿/回答和异常。既有审批提醒、实时消息浏览器回归通过；主 Agent 已审阅完整差异及关键手机截图。
- 复验：`npm test`、`node scripts/turn-process-smoke.mjs`、`node scripts/approval-reminders-smoke.mjs`、`node scripts/live-message-smoke.mjs`；浏览器脚本使用模拟 API，可用 `PLAYWRIGHT_MODULE` 指定已有模块。证据位于本地 `output/playwright/turn-process/`，包含 `unit.log`、`browser.log`、`approvals.log`、`delivery.patch` 与收起/展开/异常截图。
- 真实 Web 的四个修改资源与磁盘一致，均为 `no-store`；桌面桥接保持就绪，未重启服务，独立验证登录已退出。刷新页面即可生效。未知消息阶段保留正文，元数据缺失时不推测状态或耗时，文件数量按已加载记录注明；真实 iPhone 尚未实测。

## 模型与推理强度增补（2026-09-15）

- `/model` 菜单打开独立选择表单；模型来自本机桌面目录，推理强度随模型联动，使用中文标签并保留原生参数值。模型变更时选用该模型的目录默认强度，不沿用上一模型不支持的档位。
- `GET /api/models` 需要登录，仅返回模型展示字段、支持强度与同步时间；隐藏模型和内部提示词不返回。目录读取失败可重试，超过一天的目录标记可能过期，重读不会启动新 App Server。
- 设置按会话保留，用于后续 `turn/start` 的 `model`、`effort` 参数；执行中 `turn/steer` 不携带这些覆盖参数。确认发送成功后清除对应的下轮覆盖，失败保留，回执未知时显示待确认；旧回执不能清除后来保存的新选择。取消不修改已保存设置，消息草稿与图片保留。
- 每次打开并行读取模型目录和 `thread/read`（`includeTurns: false`）当前设置，优先使用 `thread.model`、`thread.reasoningEffort`；消费 `thread/settings/updated` 的 `threadSettings.model`、`effort`。旧读取不能覆盖新设置事件，其他会话、关闭后或重新打开前的迟到响应不能覆盖当前选择，桌面事件不能覆盖用户正在编辑的选择。
- 状态区区分当前会话、尚未发送的下轮设置和提交待确认；当前强度未知或读取失败时明确提示，不拿目录默认值冒充当前值。兼容 `/model 型号`；`/model default` 恢复首次连接的型号及该型号目录中的默认强度，不表示恢复首次连接时的自定义强度。
- 复验命令：`npm test` 和 `node scripts/model-picker-smoke.mjs`；浏览器脚本使用模拟 API，可通过 `PLAYWRIGHT_MODULE` 指定已有 Playwright。日志截图保存在本地 `output/playwright/model-picker/`。
- 同步修复验证：全套测试 103/103、composer 定向 37/37；隔离 Chrome 320/390px 通过旧快照 `xhigh` → 当前读取 `medium`、桌面设置事件和发送后覆盖清理场景，并回归选择联动、取消、会话隔离、迟到/过期/空/失败目录、命名兼容、草稿图片和模拟 RPC 参数。失败复现及修复后日志分别为 `sync-red.log`、`sync-all-tests.log`、`sync-browser.log`；主 Agent 已审阅关键差异及手机截图。
- 模型目录接口首次交付时已仅重启 Web，验证未登录 401、登录后 200、五个可展示模型及强度与桌面缓存一致，仅返回白名单字段，桌面桥接不变。此次同步修复仅涉及既有前端文件，刷新页面即可，无需重启服务。用户后续截图确认两端实际都是 `xhigh`，桌面显示“极高”、手机原显示“更高”；手机标签现已统一为“极高”，此前“桌面为中”的判断已纠正。真实 iPhone 与真实模型任务切换仍未实测。

## 待处理提醒增补（2026-09-15）

- 当前会话在输入区显示待批准、待回答数量和处理入口，点击定位请求；不主动打断阅读。其他会话在侧栏入口、项目及会话显示提醒，独立待处理列表覆盖搜索筛选外和未加载的会话。
- 请求快照与原生 `thread/status/changed` 状态按会话合并，避免重复计数；桌面处理后移除对应请求。只有原生等待状态而无可操作请求时提示“需在桌面处理”，不生成未支持的授权结果。
- 断线保留待核对提醒并禁用提交，重连后同步；旧读取结果、重复事件和桥接切换不能恢复已结束的请求。审批回答、消息草稿和图片在切换及请求更新时保留。
- 复验命令：`npm test`；`node scripts/approval-reminders-smoke.mjs`。浏览器脚本需要 Playwright 和本机 Chrome，可用 `PLAYWRIGHT_MODULE` 指定已有模块；只使用模拟 API，不连接真实任务或读取访问密码。截图位于本地 `output/playwright/approval-reminders/`。
- 隔离 Chrome 覆盖 320/390px、当前及多个请求、跨会话、过滤、未加载会话、折叠项目、慢历史、断线及恢复、桌面先处理和输入焦点保留。主 Agent 已核对关键差异和手机尺寸截图；真实 iPhone、实际桌面审批操作尚未实测。
- `npm test`：86/86；最后的滚动和连接错误局部调整后，composer 定向测试 23/23，终版隔离浏览器验证通过。等待提示已去重，重连只清除连接错误，保留其他尚未解决的操作错误。
- 已仅重启 Web 激活新模块，本机验证四个页面资源与磁盘一致，模块未登录返回 401、登录后返回 200；桌面桥接进程与标识不变，仍然就绪。验证会话已退出，未发送真实审批；手机需刷新并重新登录。

## 会话命名增补（2026-09-15）

- 新建表单增加可选名称，项目和组成部分选择保留；侧栏每条会话可直接改名，预填手工名称或首条消息生成的标题。
- 命名通过 `thread/name/set` 保存；当前标题、会话列表和本地收藏同步更新，桌面 `thread/name/updated` 事件覆盖当前及其他会话。
- 创建成功但命名失败时只重试命名；断线、回执丢失和两步之间刷新不重建会话。明确取消会清除尚未提交的命名意图并保留页内草稿；结果未知时按钮显示“关闭”，保留原回执继续查询。
- `npm test`：80/80；命名相关定向测试：29/29。`scripts/session-names-smoke.mjs` 的隔离 Chrome 验证通过，覆盖 320–1280px、新建与改名、空白/取消/失败、消息草稿和图片保留、刷新恢复及旧桥接提示。
- 主 Agent 核对本机桌面 CLI 生成的协议、关键差异及手机截图，复用交付 Agent 的有效测试结果。未修改后端或重启桌面；刷新页面即可载入前端更新。真实桌面名称显示及 iPhone 真机仍未实测。
- 复验可运行 `node --test test/composer.test.mjs test/delivery-state.test.mjs`，以及 `SESSION_NAMES_ARTIFACTS="$PWD/output/playwright/session-names" node scripts/session-names-smoke.mjs`。日志和截图保留在本地，发布文档只记录验证命令、结果及限制，不附原始任务内容。

## 要求与当前证据

| 用户要求 | 当前证据 | 尚缺证据或边界 |
| --- | --- | --- |
| 输入栏按钮一致 | Chrome 320/390/844px 验证圆形、中心线一致、触摸范围至少 44px；命令按钮不聚焦输入框 | 需在 iPhone 系统键盘和输入法下验收 |
| 手机浏览器回归 | `scripts/mobile-smoke.mjs` 覆盖布局、焦点、长文本、项目、命令、断网和设备管理 | 模拟服务不代表真实桌面任务执行 |
| 断网恢复且不重复发送 | 已接收但响应丢失后，仅查询原回执并补齐历史；失踪回执阻止新提交 | 无法找回回执时仍需用户核对桌面记录，确认后清除等待状态 |
| 项目和会话管理 | 收藏、项目展开状态、各项目起始目录偏好在刷新后保留；创建时显示项目与全部目录范围 | 偏好保存在当前浏览器，不跨设备同步；不提供电脑目录浏览器 |
| 分类命令与参数表单 | 分组展示、不可用原因、重命名表单及草稿保留经浏览器验证；目标与实时消息回归通过 | 可用命令仍受桌面桥接能力与当前任务状态限制 |
| 登录设备和安全记录 | HTTP 集成测试覆盖设备查询、撤销立即失效、请求处理中撤销及安全记录脱敏；浏览器验证设备撤销操作 | 设备名来自浏览器类型识别；安全记录有容量与采样上限，并非不可篡改审计系统 |

## 当前验证

- `npm test`：69/69 通过，覆盖认证、Broker、启动器、Web 生命周期及本轮新增行为。
- `npm run test:mobile`：通过，使用模拟 API，未执行真实任务。
- `scripts/live-message-smoke.mjs`：图片、手机/桌面消息同步与目标的模拟浏览器回归通过。
- 手机页面、项目选择、命令和设备管理截图已检查；输出在 `output/playwright/`，不进入版本管理。
- 本轮未重启桌面 App。服务仍监听 `0.0.0.0:8787`；允许的访问来源由现有配置决定，未因本轮扩展来源白名单。
- 已重启本项目 Web 服务并进行本机检查：六个页面模块/样式与磁盘一致，未登录设备查询返回 401，登录后设备查询和安全记录正常；共享桥接进程与标识保持不变，仍就绪。手机需刷新并重新登录。

## 真机验收边界

真实任务执行验收应选择可用于验证的任务，检查手机操作与桌面内容同步，以及关闭 Web 后任务继续。iPhone 系统键盘、主屏幕模式、语音权限和真实网络切换仍需真机验证。

## 手机优化回归

执行 `npm run test:mobile`。该脚本需要可导入的 Playwright 和本机 Chrome；也可将 `PLAYWRIGHT_MODULE` 指向已安装 Playwright 的 `index.mjs`。测试使用独立浏览器上下文和模拟服务，不连接真实桌面任务或读取访问密码。截图位于 `output/playwright/mobile-320.png`、`mobile-390.png`、`mobile-844.png`。

- 320、390、844 像素宽度；空闲与运行状态：按钮保持同一中心线、圆形、至少 44px，页面不横向溢出；输入与按钮之间至少 8px 间隔。
- 点命令菜单不聚焦消息输入；状态查询和参数表单保留草稿。
- 长文本自动增高；400px 高度模拟可用视口缩小时输入框有界，工具栏不重叠。
- 项目展开、收藏和可选起始目录在刷新后保留；新建默认只选项目，摘要显示目录数量，高级项可查看起始目录完整路径。
- 发送已接收但响应丢失：恢复后查询原回执、补齐历史，消息仅一条，控制RPC不重发；丢失回执时保持阻止再次发送，允许手动核对后清除。
- 回执迟到仅清理当次提交的附件，保留之后添加的图片与其他会话草稿；旧会话失效和历史重载失败不阻断连接、游标更新或审批刷新，相关 Node 回归通过。
- 设备管理展示会话并撤销指定会话；真实鉴权、撤销立即失效及日志脱敏由 Node HTTP 集成测试覆盖。

浏览器尺寸/触摸模拟不能替代 iPhone 系统键盘、Safari 主屏幕模式、真实语音权限和弱网切换。真机验收仍需：打开/收起键盘、切换中英文输入法、锁屏一分钟后返回、切换 Wi-Fi/蜂窝网络，以及后台被系统回收后重新打开。
