# 手机操作验收记录

日期：2026-09-15。负责人：主 Agent；项目/命令与设备/安全记录分别交付，主 Agent 集成并进行组合验证。
范围：本轮六项手机体验优化。HTTPS 部署仍由用户后续安排，手机真机验收不以浏览器尺寸模拟代替。

## 会话命名增补（2026-09-15）

- 新建表单增加可选名称，项目和组成部分选择保留；侧栏每条会话可直接改名，预填手工名称或首条消息生成的标题。
- 命名通过 `thread/name/set` 保存；当前标题、会话列表和本地收藏同步更新，桌面 `thread/name/updated` 事件覆盖当前及其他会话。
- 创建成功但命名失败时只重试命名；断线、回执丢失和两步之间刷新不重建会话。明确取消会清除尚未提交的命名意图并保留页内草稿；结果未知时按钮显示“关闭”，保留原回执继续查询。
- `npm test`：80/80；命名相关定向测试：29/29。`scripts/session-names-smoke.mjs` 的隔离 Chrome 验证通过，覆盖 320–1280px、新建与改名、空白/取消/失败、消息草稿和图片保留、刷新恢复及旧桥接提示。
- 主 Agent 核对本机桌面 CLI 生成的协议、关键差异及手机截图，复用交付 Agent 的有效测试结果。未修改后端或重启桌面；刷新页面即可载入前端更新。真实桌面名称显示及 iPhone 真机仍未实测。
- 复验可运行 `node --test test/composer.test.mjs test/delivery-state.test.mjs`，以及 `SESSION_NAMES_ARTIFACTS="$PWD/output/playwright/session-names" node scripts/session-names-smoke.mjs`。日志和截图保留在本地，发布文档只记录验证命令、结果及限制，不附原始任务内容。

## 要求与当前证据

| 用户要求 | 当前证据 | 尚缺证据或边界 |
| --- | --- | --- |
| 输入栏按钮一致 | Chrome 320/390/844px 验证圆形、中心线一致、触摸范围至少 44px；命令按钮不聚焦输入框 | 需在 iPhone 系统键盘和输入法下验收 |
| 手机浏览器回归 | `scripts/mobile-smoke.mjs` 覆盖布局、焦点、长文本、项目、命令、断网和设备管理 | 模拟服务不代表真实桌面任务执行 |
| 断网恢复且不重复发送 | 已接收但响应丢失后，仅查询原回执并补齐历史；失踪回执阻止新提交 | 无法找回回执时仍需用户核对桌面记录，确认后清除等待状态 |
| 项目和会话管理 | 收藏、项目展开状态、各项目组成部分选择在刷新后保留；创建时明确显示项目与工作目录 | 偏好保存在当前浏览器，不跨设备同步；不提供电脑目录浏览器 |
| 分类命令与参数表单 | 分组展示、不可用原因、重命名表单及草稿保留经浏览器验证；目标与实时消息回归通过 | 可用命令仍受桌面桥接能力与当前任务状态限制 |
| 登录设备和安全记录 | HTTP 集成测试覆盖设备查询、撤销立即失效、请求处理中撤销及安全记录脱敏；浏览器验证设备撤销操作 | 设备名来自浏览器类型识别；安全记录有容量与采样上限，并非不可篡改审计系统 |

## 当前验证

- `npm test`：69/69 通过，覆盖认证、Broker、启动器、Web 生命周期及本轮新增行为。
- `npm run test:mobile`：通过，使用模拟 API，未执行真实任务。
- `scripts/live-message-smoke.mjs`：图片、手机/桌面消息同步与目标的模拟浏览器回归通过。
- 手机页面、项目选择、命令和设备管理截图已检查；输出在 `output/playwright/`，不进入版本管理。
- 本轮未重启桌面 App。服务仍监听 `0.0.0.0:8787`；允许的访问来源由现有配置决定，未因本轮扩展来源白名单。
- 已重启本项目 Web 服务并进行本机检查：六个页面模块/样式与磁盘一致，未登录设备查询返回 401，登录后设备查询和安全记录正常；共享桥接进程与标识保持不变，仍就绪。手机需刷新并重新登录。

## 真机验收边界

真实任务执行验收应选择可用于验证的任务，检查手机操作与桌面内容同步，以及关闭 Web 后任务继续。iPhone 系统键盘、主屏幕模式、语音权限和真实网络切换仍需真机验证。

## 手机优化回归

执行 `npm run test:mobile`。该脚本需要可导入的 Playwright 和本机 Chrome；也可将 `PLAYWRIGHT_MODULE` 指向已安装 Playwright 的 `index.mjs`。测试使用独立浏览器上下文和模拟服务，不连接真实桌面任务或读取访问密码。截图位于 `output/playwright/mobile-320.png`、`mobile-390.png`、`mobile-844.png`。

- 320、390、844 像素宽度；空闲与运行状态：按钮保持同一中心线、圆形、至少 44px，页面不横向溢出；输入与按钮之间至少 8px 间隔。
- 点命令菜单不聚焦消息输入；状态查询和参数表单保留草稿。
- 长文本自动增高；400px 高度模拟可用视口缩小时输入框有界，工具栏不重叠。
- 项目展开、收藏和组成部分选择在刷新后保留；新建摘要显示项目与完整工作目录。
- 发送已接收但响应丢失：恢复后查询原回执、补齐历史，消息仅一条，控制RPC不重发；丢失回执时保持阻止再次发送，允许手动核对后清除。
- 回执迟到仅清理当次提交的附件，保留之后添加的图片与其他会话草稿；旧会话失效和历史重载失败不阻断连接、游标更新或审批刷新，相关 Node 回归通过。
- 设备管理展示会话并撤销指定会话；真实鉴权、撤销立即失效及日志脱敏由 Node HTTP 集成测试覆盖。

浏览器尺寸/触摸模拟不能替代 iPhone 系统键盘、Safari 主屏幕模式、真实语音权限和弱网切换。真机验收仍需：打开/收起键盘、切换中英文输入法、锁屏一分钟后返回、切换 Wi-Fi/蜂窝网络，以及后台被系统回收后重新打开。
